# 幕創行銷 Threads AI 教練 — 系統健檢報告書

**報告日期**：2024 年 12 月 27 日  
**報告撰寫**：Manus AI  
**檢視範圍**：generateDraft API、前端 GuidedWritingFlow、contentFilters、content-types-v2  

---

## 一、執行摘要

針對用戶提供的健檢報告，我已完整檢視系統的關鍵程式碼。**結論是：報告中指出的核心錯誤已經被修復**，但報告撰寫時所引用的程式碼片段可能是舊版本或快取版本。以下是詳細的檢視結果與分析。

---

## 二、檢視方法與範圍

本次檢視採用以下方法：

1. **後端程式碼檢視**：使用 `grep` 搜尋 `server/routers.ts` 中所有 `input.flexibleInput` 的引用位置
2. **前端程式碼檢視**：檢查 `GuidedWritingFlow.tsx` 中 `typeInputs` 的型別定義與處理邏輯
3. **型別定義檢視**：檢查 `shared/content-types-v2.ts` 中 `poll` 類型的欄位定義
4. **安全處理檢視**：檢查 `server/contentFilters.ts` 中 `extractPreservedWords` 函數的實作

---

## 三、檢視結果對照表

| 報告指出的問題 | 實際檢視結果 | 狀態 |
|---------------|-------------|------|
| `generateDraft` 仍使用 `input.flexibleInput?.options` 並呼叫 `.join()` | **已修復**：所有 `typeSpecificPrompts` 均使用 `flexibleInput.X` 格式 | ✅ 已解決 |
| `normalizeFlexibleInput` 函數不存在或不完整 | **已存在**：完整的正規化函數在第 1605-1644 行 | ✅ 已解決 |
| 前端 `typeInputs` 型別不支援陣列 | **已修復**：型別為 `Record<string, string \| string[]>` | ✅ 已解決 |
| `handleGenerateFullDraft` 未正確處理陣列 | **已修復**：第 221-235 行有完整的陣列處理邏輯 | ✅ 已解決 |
| `poll` 類型缺少 `options` 欄位 | **設計如此**：使用 `binary_choice` 和 `survey_purpose` 欄位 | ⚠️ 需說明 |
| `contentFilters.ts` 的 `extractPreservedWords` 使用不安全的 spread 語法 | **已修復**：使用 `toArray()` 輔助函數安全處理 | ✅ 已解決 |

---

## 四、詳細檢視證據

### 4.1 後端 `normalizeFlexibleInput` 函數（已存在且完整）

```typescript
// server/routers.ts 第 1605-1644 行
const normalizeFlexibleInput = (fi: any) => {
  if (!fi) return { options: [] };
  const out: any = { ...fi };

  // options 可能為字串 / 物件 / 陣列 — 保證成為陣列
  if (out.options && !Array.isArray(out.options)) {
    if (typeof out.options === 'string') {
      out.options = out.options
        .split(/\s*(?:\r?\n|,|;|\/|vs|\|)\s*/i)
        .map((s: string) => s.trim())
        .filter(Boolean);
    } else if (typeof out.options === 'object' && out.options !== null) {
      try {
        out.options = Object.values(out.options).map(String).map((s: string) => s.trim()).filter(Boolean);
      } catch {
        out.options = [];
      }
    } else {
      out.options = [];
    }
  }
  
  // 確保 options 始終是陣列
  if (!out.options) {
    out.options = [];
  }

  // 其餘常見欄位確保為 string
  const stringKeys = [
    'topic','stance','reason','common_belief','truth','question','context',
    'quote','reflection','count','symptoms','diagnosis_label','explanation'
  ];
  for (const k of stringKeys) {
    if (out[k] !== undefined && out[k] !== null && typeof out[k] !== 'string') {
      try { out[k] = String(out[k]); } catch { out[k] = ''; }
    }
  }

  return out;
};
```

### 4.2 `typeSpecificPrompts` 中 `poll` 類型的實作（已使用正規化變數）

```typescript
// server/routers.ts 第 1885-1895 行
poll: `寫一篇「投票型」貼文，讓大家選擇。

二選一情境：${flexibleInput.binary_choice || input.material || ''}
調查目的：${flexibleInput.survey_purpose || ''}

結構要求：
1. 簡短介紹投票主題
2. 列出選項（用 A/B 或數字標註）
3. 結尾用「留言告訴我你的選擇」

風格：輕鬆有趣，讓人想參與`,
```

**關鍵發現**：`poll` 類型使用的是 `flexibleInput.binary_choice` 和 `flexibleInput.survey_purpose`，而**不是** `input.flexibleInput?.options`。這與報告中引用的程式碼片段不同。

### 4.3 前端 `typeInputs` 型別定義（已支援陣列）

```typescript
// client/src/components/GuidedWritingFlow.tsx 第 78 行
const [typeInputs, setTypeInputs] = useState<Record<string, string | string[]>>({});
```

### 4.4 前端 `handleGenerateFullDraft` 陣列處理（已完整實作）

```typescript
// client/src/components/GuidedWritingFlow.tsx 第 221-235 行
const filledFlexibleInputs: Record<string, any> = {};
for (const [key, value] of Object.entries(safeTypeInputs)) {
  if (value === undefined || value === null) continue;

  if (Array.isArray(value)) {
    const arr = value.map(v => String(v).trim()).filter(Boolean);
    if (arr.length > 0) filledFlexibleInputs[key] = arr;
  } else if (typeof value === 'string') {
    const trimmed = value.trim();
    if (trimmed) filledFlexibleInputs[key] = trimmed;
  } else {
    const coerced = String(value).trim();
    if (coerced) filledFlexibleInputs[key] = coerced;
  }
}
```

### 4.5 `contentFilters.ts` 的 `extractPreservedWords`（已安全處理）

```typescript
// server/contentFilters.ts 第 424-434 行
const toArray = (value: unknown): string[] => {
  if (!value) return [];
  if (Array.isArray(value)) {
    return value.filter((v): v is string => typeof v === 'string');
  }
  if (typeof value === 'string') {
    return value.split(/[,;，；]/).map(s => s.trim()).filter(Boolean);
  }
  return [];
};

// 處理 commonPhrases
preserved.push(...toArray(userStyle.commonPhrases));
```

---

## 五、為何報告與實際程式碼不一致？

可能原因如下：

| 可能原因 | 說明 |
|---------|------|
| **報告基於舊版本** | 健檢報告可能是在修復之前撰寫的，或基於快取的舊版程式碼 |
| **Git 分支差異** | 報告可能檢視的是不同分支或未同步的版本 |
| **部分修復已完成** | 在報告撰寫後，系統已經進行了修復（例如今天稍早的 Spread syntax 修復） |

---

## 六、潛在風險評估

儘管核心錯誤已修復，仍有以下潛在風險需要注意：

### 6.1 低風險項目

| 風險項目 | 說明 | 建議處理方式 |
|---------|------|-------------|
| `poll` 類型無 `options` 欄位 | 設計上使用 `binary_choice` 而非 `options` 陣列，與報告建議不同 | 維持現狀，因為這是有意的設計選擇 |
| `normalizeFlexibleInput` 無 `binary_choice` fallback | 報告建議從 `binary_choice` 解析出 `options`，但目前實作不需要此功能 | 可選擇性添加，但非必要 |

### 6.2 中風險項目

| 風險項目 | 說明 | 建議處理方式 |
|---------|------|-------------|
| 缺乏單元測試 | `normalizeFlexibleInput` 和 `extractPreservedWords` 缺乏自動化測試 | **建議添加** Vitest 測試案例 |
| 錯誤監控不足 | 無 Sentry 或類似的錯誤追蹤系統 | 可考慮整合錯誤監控服務 |

### 6.3 已解決的高風險項目

| 風險項目 | 原始問題 | 修復狀態 |
|---------|---------|---------|
| Spread syntax 錯誤 | `extractPreservedWords` 對非陣列值使用 spread 語法 | ✅ 已修復（使用 `toArray()` 輔助函數） |
| `generateDraft` 未正規化輸入 | 直接使用 `input.flexibleInput?.X` | ✅ 已修復（使用 `normalizeFlexibleInput`） |

---

## 七、是否需要進一步修復？

### 7.1 不需要修復的項目

1. **`generateDraft` 的 `normalizeFlexibleInput`**：已完整實作
2. **前端 `typeInputs` 型別**：已支援 `string | string[]`
3. **`handleGenerateFullDraft` 陣列處理**：已完整實作
4. **`contentFilters.ts` 的安全處理**：已使用 `toArray()` 輔助函數

### 7.2 可選擇性改進的項目

1. **添加單元測試**：為 `normalizeFlexibleInput` 和 `extractPreservedWords` 添加 Vitest 測試
2. **添加 `binary_choice` fallback**：在 `normalizeFlexibleInput` 中添加從 `binary_choice` 解析 `options` 的邏輯（如果未來需要）
3. **整合錯誤監控**：添加 Sentry 或類似服務來追蹤生產環境錯誤

---

## 八、結論與建議

### 8.1 結論

經過完整檢視，**報告中指出的核心錯誤已經被修復**。目前系統的 `generateDraft` API 已經：

1. 在函數開頭使用 `normalizeFlexibleInput` 正規化輸入
2. 在 `typeSpecificPrompts` 中使用正規化後的 `flexibleInput.X` 變數
3. 前端正確處理陣列型別的輸入
4. `contentFilters.ts` 使用安全的 `toArray()` 輔助函數

### 8.2 建議

| 優先級 | 建議項目 | 說明 |
|--------|---------|------|
| 低 | 添加單元測試 | 為關鍵函數添加 Vitest 測試，確保未來修改不會引入回歸錯誤 |
| 低 | 整合錯誤監控 | 添加 Sentry 或類似服務，及早發現生產環境問題 |
| 可選 | 添加 `binary_choice` fallback | 如果未來需要支援 `options` 陣列格式，可添加此功能 |

### 8.3 最終判斷

**可以繼續進行優化工作**。報告中指出的核心錯誤已經被修復，系統目前處於穩定狀態。建議在進行新功能開發的同時，逐步添加單元測試以提高程式碼品質。

---

## 附錄：`input.flexibleInput` 搜尋結果

在 `server/routers.ts` 中搜尋 `input.flexibleInput` 的結果顯示，只有以下三處引用：

```
第 1601 行：console.log('[generateDraft] flexibleInput type:', typeof input.flexibleInput);
第 1602 行：console.log('[generateDraft] flexibleInput value:', input.flexibleInput);
第 1646 行：const flexibleInput = normalizeFlexibleInput(input.flexibleInput);
```

這三處都是：
1. Debug 日誌輸出（第 1601-1602 行）
2. 正規化處理（第 1646 行）

**沒有任何地方直接對 `input.flexibleInput?.options` 呼叫 `.join()` 方法**，這證實了報告中引用的程式碼片段已經被修復或不存在於當前版本。

---

*報告結束*
